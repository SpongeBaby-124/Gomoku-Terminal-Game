# 终端五子棋 (Gomoku Terminal)

[![PyPI version](https://badge.fury.io/py/gomoku-terminal.svg)](https://pypi.org/project/gomoku-terminal/)
[![Python](https://img.shields.io/pypi/pyversions/gomoku-terminal.svg)](https://pypi.org/project/gomoku-terminal/)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

一个基于终端的五子棋游戏，支持人机对战，使用键盘控制光标进行游戏。**现已支持与 AI（GPT/Claude）实时对战和聊天！**

## 📸 游戏截图

![Gomoku Game Screenshot](screenshots/gameplay.png)

## ✨ 特性

- 🎮 **键盘控制** - 使用方向键移动光标，空格落子
- 🤖 **三种AI难度** - 简单、中等、困难可选
- 🧠 **AI对战** - 支持 OpenAI GPT 和 Anthropic Claude 进行真正的智能对战
- 💬 **实时聊天** - 在下棋的同时与AI讨论棋局策略
- 🎨 **彩色界面** - 使用curses库实现美观的终端界面
- 🎯 **完整的游戏逻辑** - 五子连珠判定、胜负判断
- 📦 **易于安装** - 通过pip一键安装
- 🌐 **跨平台** - 支持 Windows、Linux、macOS

## 🚀 快速开始

### 安装

```bash
pip install gomoku-terminal
```

> **注意：** Windows 系统会自动安装 `windows-curses` 依赖

### 启动游戏

```bash
# 默认中等难度（传统AI）
gomoku

# 指定难度
gomoku -d easy      # 简单
gomoku -d medium    # 中等
gomoku -d hard      # 困难
```

### 🧠 使用 AI 服务对战

要与 GPT 或 Claude 进行真正的智能对战，需要配置环境变量：

```bash
# 使用 OpenAI GPT
export AI_PROVIDER=openai
export AI_API_KEY=sk-your-api-key
export AI_MODEL=gpt-4o  # 可选，默认 gpt-4o

# 或使用 Anthropic Claude
export AI_PROVIDER=anthropic
export AI_API_KEY=sk-ant-your-api-key
export AI_MODEL=claude-3-5-sonnet-20241022  # 可选

# 然后启动游戏
gomoku
```

## 🎮 游戏操作

| 按键 | 功能 |
|------|------|
| **方向键 ↑↓←→ / WASD** | 移动光标 |
| **空格键 / Enter** | 在光标位置落子 |
| **C** | 打开聊天模式 |
| **Q** | 退出游戏 |
| **R** | 重新开始 |
| **H** | 显示帮助 |

### 💬 聊天模式

在游戏过程中按 `C` 键进入聊天模式：
- 输入消息后按 **Enter** 发送
- 按 **ESC** 退出聊天模式
- AI 会根据当前棋局给出分析和建议

> **注意：** 聊天功能需要配置 AI 服务（OpenAI 或 Anthropic），且终端宽度需要足够显示聊天区域。

## 🎯 游戏规则

在 **15×15** 的棋盘上，玩家（黑棋 ●）与 AI（白棋 ○）轮流落子，率先形成横、竖、斜任意方向的连续五子者获胜。

### 符号说明

游戏会自动检测终端是否支持 Unicode 字符：
- **支持 Unicode**：`●`（黑棋）、`○`（白棋）、`◆`（光标）、`·`（空位）
- **不支持 Unicode**：`X`（黑棋）、`O`（白棋）、`#`（光标）、`+`（空位）

### 光标提示

- 光标位置以**反色高亮**显示（使用下划线和加粗效果）
- 即使该位置已有棋子，也能清晰看到光标
- 状态栏实时显示当前光标坐标（如 `A1`, `M13` 等）

## 🤖 AI 难度说明

### 传统 AI（无需 API）

| 难度 | 算法 | 特点 | 适合玩家 |
|------|------|------|---------|
| **Easy** | 基础策略 | 简单攻守，有一定威胁识别 | 新手玩家 |
| **Medium** | 评分系统 | 全局评估，攻守平衡 | 普通玩家 |
| **Hard** | Minimax + Alpha-Beta | 深度搜索，步步为营 | 高级玩家 |

### AI 服务对战

| 提供商 | 模型 | 特点 |
|--------|------|------|
| **OpenAI** | gpt-4o, gpt-4, gpt-3.5-turbo | 强大的推理能力，支持复杂局面分析 |
| **Anthropic** | claude-3-5-sonnet, claude-3-opus | 出色的策略思考，回复风格友好 |

## ⚙️ AI 配置详解

### 环境变量

| 变量名 | 必填 | 说明 | 示例 |
|--------|------|------|------|
| `AI_PROVIDER` | 是 | AI 提供商 | `openai`, `anthropic`, `traditional` |
| `AI_API_KEY` | 是* | API 密钥 | `sk-...` |
| `AI_MODEL` | 否 | 模型名称 | `gpt-4o`, `claude-3-5-sonnet-20241022` |
| `AI_ENDPOINT` | 否 | 自定义端点 | `https://api.openai.com/v1` |
| `AI_TIMEOUT` | 否 | 超时时间（秒） | `30` |
| `AI_MAX_RETRIES` | 否 | 最大重试次数 | `3` |

> *使用 `traditional` 模式时不需要 API 密钥

### 获取 API 密钥

- **OpenAI**: https://platform.openai.com/api-keys
- **Anthropic**: https://console.anthropic.com/

### 降级策略

如果 AI 服务不可用（网络问题、API 错误等），游戏会自动降级到传统 AI，确保游戏体验不受影响。

## 🛠️ 开发

### 从源码运行

```bash
git clone https://github.com/SpongeBaby-124/Gomoku-Terminal-Game.git
cd Gomoku-Terminal-Game
pip install -e .
pip install openai anthropic  # 安装 AI 依赖
python -m gomoku
```

### 运行测试

```bash
pytest tests/
```

### 项目结构

```
gomoku/
├── __init__.py
├── __main__.py         # 入口点
├── board.py            # 棋盘逻辑
├── game.py             # 游戏主循环
├── ui.py               # 终端界面（含聊天）
├── ai.py               # 传统 AI 算法
├── config.py           # 配置管理
├── ai_provider.py      # AI 服务抽象层
├── ai_service.py       # AI 服务工厂
├── chat_manager.py     # 聊天管理
├── logger.py           # 日志记录
└── providers/          # AI 提供商实现
    ├── openai_provider.py
    └── anthropic_provider.py
```

## 📦 技术栈

- **语言**：Python 3.8+
- **UI库**：curses / windows-curses
- **AI算法**：Minimax + Alpha-Beta 剪枝
- **AI服务**：OpenAI API, Anthropic API
- **打包**：setuptools, build, twine

## 🐛 常见问题

### 聊天区域不显示？
- 确保终端窗度至少 80 列宽
- 尝试调整终端窗口大小

### AI 落子很慢？
- 检查网络连接
- 可以调小 `AI_TIMEOUT` 值
- 考虑使用更快的模型（如 gpt-3.5-turbo）

### API 密钥错误？
- OpenAI 密钥应以 `sk-` 开头
- Anthropic 密钥应以 `sk-ant-` 开头
- 确保密钥有效且有足够额度

### 日志在哪里？
- 日志文件位于 `~/.gomoku/logs/` 目录
- 可用于调试 AI 服务问题

## 🤝 贡献

欢迎贡献！请随时提交 Pull Request。

1. Fork 本仓库
2. 创建你的特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交你的改动 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 打开一个 Pull Request

## 📄 许可证

本项目采用 MIT 许可证 - 详见 [LICENSE](LICENSE) 文件

## 🔗 链接

- **PyPI**: https://pypi.org/project/gomoku-terminal/
- **GitHub**: https://github.com/SpongeBaby-124/Gomoku-Terminal-Game

## ⭐ Star History

如果你觉得这个项目不错，请给它一个 Star！

---

Made with ❤️ by Python & Curses
